[project]
name = "llm-cli-chat"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.27.2",
]
[build-system]
requires = ["setuptools>=61.0"]
build-backend= "setuptools.build_meta"
[tool.setuptools]
packages = ["src"]